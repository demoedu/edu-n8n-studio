# Hugging Face Papers Daily Crawler

매일 오전 9시에 Hugging Face Papers 페이지를 크롤링하여 슬랙으로 알림을 보내는 n8n 워크플로우입니다.

## 워크플로우 구성

### 1. Schedule Trigger
- **실행 시간**: 매일 오전 9시 (KST)
- **Cron 표현식**: `0 9 * * *`
- **타임존**: Asia/Seoul

### 2. Fetch HF Papers (HTTP Request)
- **URL**: https://huggingface.co/papers
- **Method**: GET
- **기능**: Hugging Face Papers 페이지의 HTML을 가져옵니다

### 3. Parse Papers (Code Node)
- **언어**: JavaScript
- **기능**:
  - HTML에서 논문 제목과 링크를 정규식으로 추출
  - 상위 10개 논문만 선택
  - 각 논문의 제목, 링크, 날짜 정보를 JSON으로 변환

### 4. Send to Slack (Slack Node)
- **Resource**: Message
- **Operation**: Post
- **Channel**: #general (변경 가능)
- **메시지 형식**:
  ```
  📚 *Hugging Face Daily Papers - 2025-10-17*

  *논문 제목*
  🔗 https://huggingface.co/papers/...

  ---
  ```

## 사용 방법

### 1. 워크플로우 활성화
n8n UI에서 워크플로우를 활성화하면 매일 자동으로 실행됩니다.

### 2. Slack 연동 설정
- n8n에서 Slack API 자격증명을 추가해야 합니다
- Slack 앱 생성 및 필요한 권한 부여:
  - `chat:write` - 메시지 전송
  - `channels:read` - 채널 정보 읽기

### 3. 채널 변경
Slack 노드에서 `channelId` 값을 원하는 채널명으로 변경:
```json
{
  "__rl": true,
  "mode": "name",
  "value": "your-channel-name"
}
```

### 4. 크롤링 개수 조정
Parse Papers 노드의 코드에서 `count < 10` 부분을 수정하여 크롤링할 논문 개수를 변경할 수 있습니다.

### 5. 실행 시간 변경
Schedule Trigger 노드의 cron 표현식을 수정:
- 매일 오후 6시: `0 18 * * *`
- 매주 월요일 오전 9시: `0 9 * * 1`
- 매시간: `0 * * * *`

## 워크플로우 ID
- **ID**: efYpmPuLDSauwdD7
- **이름**: Hugging Face Papers Daily Crawler

## 검증 상태
✅ 워크플로우 검증 완료 (valid: true)
- 총 노드: 4개
- 연결: 3개 (모두 유효)
- 오류: 0개

## 주의사항

### 경고 사항
1. **에러 핸들링**: 프로덕션 환경에서는 각 노드에 에러 핸들링 추가 권장
2. **Rate Limiting**: Slack API 호출 제한에 유의
3. **HTML 구조 변경**: Hugging Face 페이지 구조가 변경되면 정규식 수정 필요

### 개선 제안
1. HTTP Request 노드에 `retryOnFail: true` 추가
2. Error Trigger 노드 추가하여 실패 시 알림
3. 더 정교한 HTML 파싱을 위해 cheerio 라이브러리 사용 고려 (외부 모듈 허용 시)

## 데이터 형식

각 논문은 다음 형식으로 처리됩니다:
```json
{
  "title": "논문 제목",
  "link": "https://huggingface.co/papers/...",
  "date": "2025-10-17"
}
```

## 문제 해결

### 논문이 추출되지 않는 경우
1. Hugging Face 페이지 HTML 구조가 변경되었는지 확인
2. Parse Papers 노드의 정규식 패턴 업데이트 필요

### Slack 메시지가 전송되지 않는 경우
1. Slack 자격증명 확인
2. 채널명이 정확한지 확인
3. Slack 앱 권한 확인

### 스케줄이 실행되지 않는 경우
1. 워크플로우 활성화 상태 확인
2. Cron 표현식 확인
3. 타임존 설정 확인
